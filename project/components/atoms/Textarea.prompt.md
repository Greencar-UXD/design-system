Textarea — multi-line text field, vertically resizable, with the same label/hint/error contract as Input.

```jsx
import { Textarea } from './components/forms/Textarea';

<Textarea label="소개" placeholder="자기소개를 입력하세요" rows={5} />
<Textarea label="메모" error="필수 항목입니다" required />
```
