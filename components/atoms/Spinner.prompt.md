Spinner — circular loading indicator with a cobalt arc.

```jsx
import { Spinner } from './components/feedback/Spinner';

<Spinner size="sm" />
<Spinner />          {/* md */}
<Spinner size="lg" />
```

- **size**: `sm` (16) · `md` (24) · `lg` (36). Always carries an aria label.
