Radio — labeled radio button. Group options by sharing a `name`.

```jsx
import { Radio } from './components/forms/Radio';

<Radio name="plan" label="무료" defaultChecked />
<Radio name="plan" label="프로" />
```
